var expect = require('chai').expect;
var HashTable = require('./hash_table');

describe('HashTable', function() {
  it('is defined', function() {
    expect(typeof HashTable).to.equal('function');
  });
});

